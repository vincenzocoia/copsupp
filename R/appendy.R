
appendy <- function(dats, xvine, yvar, QY, startQY, tauset) {
    dattrain <- dats$train
    datval <- dats$val
    ## Fit QY
    if (length(startQY) == 0) {
        QYfit <- QY
    } else {

    }
}
